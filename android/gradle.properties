org.gradle.jvmargs=-Xmx3G -XX:MaxMetaspaceSize=1G -XX:ReservedCodeCacheSize=256m -XX:+HeapDumpOnOutOfMemoryError
org.gradle.workers.max=2
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Pub cache (C:) and project (E:) are on different drives; Kotlin's incremental
# compiler can't relativize paths across Windows drive roots and crashes closing
# its caches. Disabling incremental compilation avoids that code path.
kotlin.incremental=false
